# Supported Regions: ap-northeast-2 or us-east-1. Same value as the profile region.
aws_region   = "ap-northeast-2"
project_name = "mini-commerce"

# Enable only in disposable accounts. Production accounts keep false.
force_destroy = true

tags = {
  Owner = "replace-with-owner"
}
